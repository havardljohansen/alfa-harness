import type { RelayAssignment, FuseBlock } from "./types";

const SPST = "301-1A-C-R1-U03-12VDC"; // 6 owned
const SPDT = "301-1C-S-R1-12VDC"; // 5 owned

// ===========================================================================
// Fuse / relay centres — all mounted in the engine bay per the build.
// ===========================================================================
export const fuseBlocks: FuseBlock[] = [
  {
    id: "pdm",
    name: "Headlight power centre (PDM)",
    model: "Littelfuse HWB18 PDM31002ZXM",
    partRef: "PDM31002ZXM",
    bussed: false,
    zone: "engine-front",
    fuseWays: 5,
    relayWays: 2,
    grid: { fuseCols: 3, relayCols: 2 },
    note: "18 cavities on a 6×3 grid = 9 fuse-equivalents; a relay eats 2 positions (2×relays + fuses ≤ 9, max 3 relays). We run the 2R/5F config: low + high beam relays (commons fed from the main input) + 4 beam-output fuses, leaving ONE spare fuse slot. 100 A, IP69K. WHY THE PDM EXISTS: the two Bussmanns are at/near relay capacity (ign 5/5, const 4/5 — one free slot between them), so the headlights' 2 relays + 4 fuses don't fit without a third RTMR. The PDM offloads the whole headlight section, AND sits forward at the front clip so the heavy beam outputs stay there (only thin triggers cross BH4). WHY 2R/5F NOT 3R/3F: keeping each beam filament on its own fuse means a blown fuse drops one beam, not both (night-safety) — worth more than a 3rd relay. The free relay slot stays a documented spare; the horn relay lives on the constant RTMR, not here.",
  },
  {
    id: "rtmr-ign",
    name: "Cabin IGNITION bus (RTMR, bussed)",
    model: "Bussmann RTMR 15306-2-2-4 (bussed)",
    partRef: "15306-2-2-4",
    bussed: true,
    zone: "engine-front",
    fuseWays: 10,
    relayWays: 5,
    grid: { fuseCols: 5, relayCols: 5 },
    note: "Input stud fed by the ignition main relay. All positions hot only in RUN.",
  },
  {
    id: "rtmr-const",
    name: "CONSTANT (battery) bus (RTMR, bussed)",
    model: "Bussmann RTMR 15306-2-2-4 (bussed)",
    partRef: "15306-2-2-4",
    bussed: true,
    zone: "engine-front",
    fuseWays: 10,
    relayWays: 5,
    grid: { fuseCols: 5, relayCols: 5 },
    note: "Input stud fed direct from the battery. Hot at all times (hazard, horn, brake, tail/position, interior). Cavity allocation: 1=turn-L, 2=turn-R, 3=horn, 4=starter, 5=flasher (Bussmann NO-762-LED, ISO-280-socket flasher — see components.ts/flasher and engine.ts cavity bus edge). All 5 cavities occupied; the future washer relay was evicted to external mounting when this swap happened.",
  },
];

// ===========================================================================
// Relay allocation — 6 SPST + 5 SPDT = the 11 owned, all in use NOW. Plus one
// FUTURE SPST (washer) provisioned in the spare constant-RTMR slot — bought and
// fitted with the deferred electric washer pump, so it isn't counted as owned.
// ISO-280 pinout: 85/86 coil, 30 common, 87 NO, 87a NC (SPDT only).
// "as much as possible on relays" — every meaningful load is relay-driven and
// every tired switch carries only coil current.
// ===========================================================================
export const relays: RelayAssignment[] = [
  // --- SPST (6 in use + 1 future washer) ------------------------------------
  {
    id: "rly-low",
    name: "Low-beam relay",
    type: "SPST",
    fn: "Feeds both H4 low filaments. Coil is fed by sw-headlight LOW output (constant feed via dash switch — so the coil energises in any rotary position with lever UP at the headlights notch, key or no key). Common is gated by IGNITION via a separate wire from f-ign-6 (rtmr-ign), bypassing the PDM's main constant input. Effect: with key off + dash at LOW, the coil clicks but the bulbs don't light because the common is dead. This is what makes 'set LOW and forget' work — low beams die with the key, no drain.",
    mountedIn: "pdm",
    partRef: SPST,
    coilFrom: "switched",
    coilTriggerLabel: "HL.LO.TRG",
    commonFrom: "switched",
    out87: "Both low beams (HL.LO.OUT.L / .R)",
    note: "PDM HWB18 supports per-relay-common external wiring (commons aren't bussed internally on this part) — this is what allows rly-low to take its common from ign-bus while rly-high stays on the PDM's main constant input. NB: the propagation engine (engine.ts staticEdges) deliberately omits the pdm.BUS → rly-low.30 internal edge that it has for rly-high — search 'pdm.BUS → rly-high.30' to find the asymmetry. Don't add the rly-low edge back without changing this architecture.",
  },
  {
    id: "rly-high",
    name: "High-beam relay",
    type: "SPST",
    fn: "Feeds both H4 high filaments + blue tell-tale. Coil from dip/flash switch, gated by ignition position I.",
    mountedIn: "pdm",
    partRef: SPST,
    coilFrom: "ign-pos1",
    coilTriggerLabel: "HL.HI.TRG",
    commonFrom: "battery",
    out87: "Both high beams (HL.HI.OUT.L / .R) + main-beam tell-tale",
  },
  {
    id: "rly-horn",
    name: "Horn relay",
    type: "SPST",
    fn: "Carries the horn load; horn button only grounds the coil.",
    mountedIn: "rtmr-const",
    partRef: SPST,
    coilFrom: "battery",
    coilTriggerLabel: "HORN.TRG",
    commonFrom: "battery",
    out87: "Both horns (HORN.OUT)",
  },
  {
    id: "rly-fa",
    name: "Fan-adapter speed-select relay (SPDT)",
    type: "SPDT",
    fn: "Lives INSIDE the fan-adapter module (behind the fc 4-pin connector) — not on the chassis loom. Single SPDT replaces what would otherwise be two SPSTs: COM (30) sees gated 12V from fc pin 1, NC (87a) defaults to LOW winding, NO (87) routes to HIGH winding when the coil is energised by fc pin 3 (HIGH signal from the dash switch). Mutual exclusion is mechanical — the COM can only be on one contact at a time, so both windings can never see 12V simultaneously. Failure mode: if the coil burns out, relay defaults to NC = LOW winding still works (graceful degradation, fan keeps running on slow).",
    mountedIn: "fan-adapter",
    partRef: SPDT,
    future: true,
    coilFrom: "switched",
    coilTriggerLabel: "FAN.HI.SIG",
    commonFrom: "switched",
    out87: "Heater-fan HIGH winding (FAN.HI.OUT)",
    out87a: "Heater-fan LOW winding (FAN.LO.OUT — default position, no coil power)",
    note: "Provisioned: marked future because the fan-adapter sub-build is sourced separately from the chassis loom (same way rly-washer is provisioned). +1 SPDT needed beyond the 5 owned for chassis use — order alongside the fc connector and the 1N4007 diode pair. Same Song Chuan 301-1C-S-R1 SPDT as the rest of the harness, OR a small PCB-mount automotive SPDT rated for 15 A if the adapter is built on a board.",
  },
  {
    id: "rly-fan",
    name: "Heater-fan gate relay",
    type: "SPST",
    fn: "Single relay in the Bussmann that gates 12V to the fan-adapter module via the `fc` 4-pin connector. Coil triggered by signal-join: both sw-heaterfan.low and sw-heaterfan.high tied together at the fc connector → energise this coil whenever the switch is in either non-OFF position. The fan-adapter behind fc handles speed selection (its own internal relays for the 3-wire fan variant; pass-through for 2-wire or 4-wire smart-fan variants). Common fed from the CONSTANT bus so the ~15 A motor doesn't pass through the ignition-main relay; coil is implicitly ignition-gated by the switch supply.",
    mountedIn: "rtmr-ign",
    partRef: SPST,
    coilFrom: "switched",
    coilTriggerLabel: "FAN.TRG",
    commonFrom: "battery",
    out87: "Heater-fan gate output → fc connector pin 1 (FAN.OUT)",
  },
  {
    id: "rly-fuel",
    name: "Fuel-pump relay",
    type: "SPST",
    fn: "Carries the low-pressure carb fuel pump; coil straight off the ignition bus (runs key-on).",
    mountedIn: "rtmr-ign",
    partRef: SPST,
    coilFrom: "ign-run",
    coilTriggerLabel: "FUEL.TRG",
    commonFrom: "bus",
    out87: "Fuel pump (FUEL.OUT)",
    note: "Plain ignition-switched — simplest robust option for a low-pressure carb pump. Optional inertia cut-off only worth it for a high-pressure EFI pump.",
  },
  {
    id: "rly-ignmain",
    name: "Ignition main relay",
    type: "SPST",
    fn: "Offloads the worn ignition switch — switch only triggers the coil; relay feeds the whole ignition bus.",
    mountedIn: "rtmr-ign",
    partRef: SPST,
    coilFrom: "ign-run",
    coilTriggerLabel: "IGN.MAIN.TRG",
    commonFrom: "battery",
    out87: "Ignition bus input stud (rtmr-ign)",
  },
  {
    id: "rly-washer",
    name: "Washer-pump relay (FUTURE / provisioned)",
    type: "SPST",
    fn: "Keeps the period-correct dash washer button a low-current TRIGGER — the relay carries the modern electric pump. Mounts EXTERNALLY next to the RTMR when the deferred pump is fitted (the LED flasher took the last cavity).",
    mountedIn: "external-engine-front",
    partRef: SPST,
    future: true,
    coilFrom: "switched",
    coilTriggerLabel: "WASH.TRG",
    commonFrom: "bus",
    out87: "Electric washer pump (WASH.OUT)",
    note: "Provisioned: not bought yet (the 11 owned relays cover everything in use now — this is a +1 SPST to buy with the pump). EVICTED from rtmr-const cavity 5 on 2026-05-28 when the Bussmann NO-762-LED flasher moved into that cavity. When fitted, mount this relay external to the RTMR (panel-mount socket or DIN-rail base) and wire it conventionally. Vintage-look dash controls stay triggers; the modern pump hides behind this relay.",
  },

  // --- SPDT (5) -------------------------------------------------------------
  {
    id: "rly-turnL",
    name: "Turn-signal LEFT relay",
    type: "SPDT",
    fn: "Carries left indicator bulbs; weak column switch only triggers the coil. Hazard also triggers via diode.",
    mountedIn: "rtmr-const",
    partRef: SPDT,
    coilFrom: "switched",
    coilTriggerLabel: "TURN.L.TRG",
    commonFrom: "switched", // flasher output
    out87: "Left turn lamps F+R + green tell-tale (TURN.L.OUT)",
  },
  {
    id: "rly-turnR",
    name: "Turn-signal RIGHT relay",
    type: "SPDT",
    fn: "Carries right indicator bulbs; weak column switch only triggers the coil. Hazard also triggers via diode.",
    mountedIn: "rtmr-const",
    partRef: SPDT,
    coilFrom: "switched",
    coilTriggerLabel: "TURN.R.TRG",
    commonFrom: "switched",
    out87: "Right turn lamps F+R + green tell-tale (TURN.R.OUT)",
  },
  {
    id: "rly-wlow",
    name: "Wiper LOW + self-park relay",
    type: "SPDT",
    fn: "Vintage-style wiper switch triggers this coil (LOW). Energised → motor 53 (run). De-energised (switch Off) → NC contact feeds 53a so the Bosch motor self-parks. Switch carries only coil current.",
    mountedIn: "rtmr-ign",
    partRef: SPDT,
    coilFrom: "switched",
    coilTriggerLabel: "WIPE.LO.TRG",
    commonFrom: "bus",
    out87: "Wiper low / run feed → motor 53",
    out87a: "Wiper park feed → motor 53a (self-park when Off)",
  },
  {
    id: "rly-whigh",
    name: "Wiper HIGH relay",
    type: "SPDT",
    fn: "Wiper switch HIGH triggers this coil; relay feeds motor 53b. Switch carries only coil current.",
    mountedIn: "rtmr-ign",
    partRef: SPDT,
    coilFrom: "switched",
    coilTriggerLabel: "WIPE.HI.TRG",
    commonFrom: "bus",
    out87: "Wiper high feed → motor 53b",
  },
  {
    id: "rly-starter",
    name: "Starter relay",
    type: "SPDT",
    fn: "Offloads the ignition switch — the worn switch only triggers the coil (DIN 50); the relay passes battery to the starter solenoid. Solenoid pull-in (~10–20 A) is well within the 35 A relay.",
    mountedIn: "rtmr-const",
    partRef: SPDT,
    coilFrom: "ign-start",
    coilTriggerLabel: "STRT.TRG",
    commonFrom: "battery",
    out87: "Starter solenoid (DIN 50)",
  },
];
